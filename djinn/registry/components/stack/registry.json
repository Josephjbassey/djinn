{
  "name": "stack",
  "dependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "name": "stack.html",
      "dir": "templates/components",
      "content": "<div class=\"flex\n    {% if direction == 'horizontal' %}flex-row items-center space-x-{{ space|default:4 }}\n    {% else %}flex-col space-y-{{ space|default:4 }}{% endif %}\n    {{ class }}\"\n>\n    {{ slot|safe }}\n</div>\n"
    },
    {
      "name": "stack.py",
      "dir": "templatetags",
      "content": "from django import template\nfrom django.utils.html import escape\nfrom django.utils.safestring import mark_safe\n\nregister = template.Library()\n\ndef format_html_attrs(attrs):\n    attr_list = []\n    for key, attr_value in attrs.items():\n        attr_list.append(f'{key.replace(\"_\", \"-\")}=\"{escape(str(attr_value))}\"')\n    return mark_safe(\" \".join(attr_list))\n\n@register.inclusion_tag('components/stack.html')\ndef djinn_stack(class_name=\"\", **attrs):\n    \"\"\"\n    Djinn Stack component tag.\n    \"\"\"\n    return {\n        \"class\": class_name,\n        \"attrs\": format_html_attrs(attrs),\n    }\n"
    }
  ],
  "type": "components:ui"
}