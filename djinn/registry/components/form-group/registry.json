{
  "name": "form-group",
  "dependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "name": "form-group.html",
      "dir": "templates/components",
      "content": "<div class=\"space-y-2 {{ class }}\">\n    {% if label %}\n        {% firstof id name as target_id %}\n        {% include \"components/label.html\" with for=target_id label=label required=required %}\n    {% endif %}\n\n    {{ slot|safe }}\n\n    {% if help_text %}\n        <p class=\"text-xs text-muted-foreground\">{{ help_text }}</p>\n    {% endif %}\n\n    {% if error %}\n        <p class=\"text-xs font-medium text-destructive\">{{ error }}</p>\n    {% endif %}\n</div>\n"
    },
    {
      "name": "form-group.py",
      "dir": "templatetags",
      "content": "from django import template\nfrom django.utils.html import escape\nfrom django.utils.safestring import mark_safe\n\nregister = template.Library()\n\ndef format_html_attrs(attrs):\n    attr_list = []\n    for key, attr_value in attrs.items():\n        attr_list.append(f'{key.replace(\"_\", \"-\")}=\"{escape(str(attr_value))}\"')\n    return mark_safe(\" \".join(attr_list))\n\n@register.inclusion_tag('components/form-group.html')\ndef djinn_form_group(class_name=\"\", **attrs):\n    \"\"\"\n    Djinn Form-group component tag.\n    \"\"\"\n    return {\n        \"class\": class_name,\n        \"attrs\": format_html_attrs(attrs),\n    }\n"
    }
  ],
  "type": "components:ui"
}